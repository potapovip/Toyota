public abstract class PassengerCar extends Car{
    public PassengerCar(String color, int maxSpeed, String gearboxType, boolean isMoving, double price) {
        super(color, maxSpeed, gearboxType, isMoving, price);
    }
}
