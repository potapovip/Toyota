public class Solara extends Cabriolet{
    public Solara(String color, int maxSpeed, String gearboxType, boolean isMoving) {
        super(color, maxSpeed, gearboxType, isMoving);
    }
//    кабриолет
}
