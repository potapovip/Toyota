public abstract class Cabriolet extends Car{

    public Cabriolet(String color, int maxSpeed, String gearboxType, boolean isMoving, double price) {
        super(color, maxSpeed, gearboxType, isMoving, price);
    }
}
