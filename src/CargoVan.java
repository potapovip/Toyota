public abstract class CargoVan extends Car{
    public CargoVan(String color, int maxSpeed, String gearboxType, boolean isMoving, double price) {
        super(color, maxSpeed, gearboxType, isMoving, price);
    }
}
