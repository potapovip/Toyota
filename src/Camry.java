public class Camry extends PassengerCar{
    public Camry(String color, int maxSpeed, String gearboxType, boolean isMoving) {
        super(color, maxSpeed, gearboxType, isMoving);
    }
//(легковой авто)
}
